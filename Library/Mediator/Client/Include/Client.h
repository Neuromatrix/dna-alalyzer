#include <fstream>
namespace Library::Client{
    class Subscriber
    {
    private:
        /* data */
    public:
        void Read();
        Subscriber(/* args */);
        ~Subscriber();
    };
}

