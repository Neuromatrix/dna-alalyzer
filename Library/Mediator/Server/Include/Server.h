#include <fstream>
namespace Library::Server{
    class Publisher
    {
    private:
        /* data */
    public:
        void Post();
        Publisher(/* args */);
        ~Publisher();
    };
}

